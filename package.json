{
  "name": "arcade-ctc",
  "version": "0.1.0",
  "private": true,
  "description": "Creditcoin Attestcoin arcade — Fruit Merge + Nutty Rider with an on-chain economy",
  "type": "module",
  "workspaces": [
    "worker",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "worker": "npm run dev -w worker",
    "backend": "npm run dev -w backend"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
